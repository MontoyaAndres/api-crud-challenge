const { CustomerController } = require('./customers');
const { ProductController } = require('./products');
const { OrderController } = require('./orders');

module.exports = { CustomerController, ProductController, OrderController };
